# Contributing

Contributions are welcome. Keep changes focused and, before opening a pull
request, run:

```sh
pnpm typecheck
cargo test --workspace
```

Use a short, sentence-case commit subject. Prefer `Area: description` when a
scope is useful, for example `Reader: fix relative link navigation`. Follow it
with a brief body describing why the change was made and any useful context.
